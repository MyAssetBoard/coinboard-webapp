<% if (locals.data && Object.keys(data).length) {%>
	<%- include('profile_card') %>
		<% for (var elems in data) {%>
			<li label="<%=elems%>">
				<%= elems != "assets" ? elems + ' : ' + data[elems] : '' %>
			</li>
		<%}
	%>
	<%
} else {%>
		<h1 class="text-white">=>Hey ! U must
			<a href="/login">log in</a>
			or
			<a href="/signin">register</a>
			before reaching
			<%=title%>
		</h1>
	<% } %>