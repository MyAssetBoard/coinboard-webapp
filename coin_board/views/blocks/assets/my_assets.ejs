<div class="container-fluid">
	<div class="row">
		<%- include('leftpanel_block') %>
			<div class="col-md-9">
				<% if (locals.routes && Object.keys(routes).length)
			{ %>
					<!-- Page Blocks inclusion -->
					<% for (var elems in routes)
				{ %>
						<%- include(routes[elems]) %><% } %>
					<%
			} %>
				</div>
			</div>
		</div>